#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: first number
 * @b: second number
 * Return: mul of a & b
 */
int mul(int a, int b)
{
	int value = a * b;

	return (value);
}
