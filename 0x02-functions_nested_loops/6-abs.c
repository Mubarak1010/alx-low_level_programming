#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 * @int: return type of our value
 * Return: abs value of n
 */
int _abs(int)
{
	int a;

	if (a >= 0)
	{
		return (a);
	}
	return (-a);
}
