#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 * @a: value to inspect
 * Return: abs value of a
 */
int _abs(int a)
{
	int a;

	if (a >= 0)
	{
		return (a);
	}
	return (-a);
}
