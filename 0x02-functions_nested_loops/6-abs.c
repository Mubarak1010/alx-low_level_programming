#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 * @n: value to inspect
 * Return: abs value of n
 */
int _abs(int n)
{
	int a;

	if (a >= 0)
	{
		return (a);
	}
	return (-a);
}
