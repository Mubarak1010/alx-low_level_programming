#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @n: first value
 * @a: second value
 * Return: a + b
 */
int add(int n, int a)
{
	int c;

	c = n + a;

	return (c);
}
